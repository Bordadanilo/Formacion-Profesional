function obtenerPrimerStringLargo(array) {
  // Devuelve el primer string con más de 5 caracteres en el array.
  // Tu código:
  
  let primerStringLargo = array.find(string => string.length > 5);
}

module.exports = obtenerPrimerStringLargo;
